/*js selectors*/

document.getElementById();// selects html element by its id 
document.getElementsByClassName();// selects html element by its class name 
document.getElementsByTagName();// selects html element with the spesific tag
document.querySelector();//selects the first element of html elements by either its name or its class name (.className) or id (#Id)
document.querySelectorAll();//selects All elements of html elements by either its name or its class name (.className) or id (#Id)


/*to convert an html collection to an array */
let test ;
test = Array.from(test)